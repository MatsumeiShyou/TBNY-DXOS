## AGENTS.md

> このファイルは、このリポジトリでAIエージェントが作業する際の唯一の正典（SSOT）である。作業開始前に必ず読むこと。
> 過去の設計判断に迷った場合は `governance/lexicon.json` と `governance/ADR/` を確認する。推測で埋めない。

---

## 1. 絶対原則 (Exceptions NOT Allowed)

1. **破壊的コマンド禁止**: 強制的な一括削除・上書きコマンド（`git clean -fdX` 等）は実行しない。
2. **ハードコード禁止**: シークレット・APIキー・個人環境固有のパス等をコードや本ファイルにハードコードしない。不明な情報は「不明」と明示する。
3. **推測による変更禁止**: 既存の関数・UI・データ構造・挙動を削除/リネーム/無効化しない。変更時は先に参照元と理由を確認する。
4. **事実確認の徹底**: 事実確認をせずに実装・提案・結論出しをしない。必ずコード・ドキュメントを検索し、必要に応じてブラウザで実証する。
5. **DB・統治構造の保護**: DBスキーマ、認証・権限、決済、および本ファイル自体（統治構造）の変更は、人間の明示的承認なしに実行しない。
6. **フォールバック禁止**: 設定の読み込みに失敗した場合、デフォルト値へ黙ってフォールバックせず、エラーを出力して停止する。
7. **孤児データの禁止**: すべての案件(Job)は必ず顧客マスタ(collection_point_id)に紐づく。AIがプロンプトで回避せず、DB制約(FK/NOT NULL)に従うこと。

## 2. 開発コマンド・フック

| コマンド | 用途 |
|---|---|
| `npm run type-check` | TypeScript型チェック（必ず実装後に実行） |
| `npm run agent:scan` | 必須ドキュメントとADRの存在チェック |
| `node scripts/check_tier_paths.mjs` | Tier自動判定チェック（コミット前に実行） |
| `npm run done` | 完了処理（GSEALコードを出力） |
| `npx supabase db diff` | スキーマ差分SQL生成 |

## 3. リスクベース・ティア判定 (Tier System)

AIの作業は以下のリスクスコア(T1〜T3)に基づき実行される。対象パスに基づく自動判定(`governance/core_config.json` の `T3_force_paths`)を優先する。

- **T1 (Light / 軽微)**: タイポ、UI微調整。AIは即実行し一言報告。ログ記録必須。
- **T2 (Standard / 標準)**: ロジック変更。バグを再現する新規回帰テスト追加 ＋ 既存CI通過で完了。
- **T3 (Strict / 厳格)**: DB変更、統治構造変更、基幹処理変更。**[State]/[Decision]/[Reason]/[Risk]/[Unknown]** フォーマットでの事前提案と、人間の監査承認（`y`）が必須。

## 4. プロダクト・データ制約 (Invariants)

- **依存方向**: `components/` → `hooks/` / `utils/` / `services/` → `data/` の一方向。循環参照は不可。
- **AI権限**: AIは可能な限り通常のRLS権限で作業し、Service Role Keyを利用した操作（スキーマ変更等）はT3として扱う。
- **並行編集 (ADR-003)**: 配車操作の競合は自動マージせず、「悲観的ロック(ドラッグ中Jobへのロック)」とUI表示で解決する。
- **データハイブリッド (ADR-001)**: Jobはフロントでシリアライズされ、マスタはSupabaseに保存される（将来的にPowerSync等への移行を検討中）。

## 5. 完了・監査手順

1. `npm run type-check` の通過確認。
2. `README.md` または `SCHEMA_HISTORY.md` の更新（必要な場合）。
3. `npm run done` を実行し `.agents/scratch/AMPLOG.jsonl` への記録。
## 6. 既知の注意点 (Caveats)
詳細な技術的負債や仕様の注意点（Lint警告の許容、孤児データの保存ガードレール等）については、.agents/rules/known_caveats.md を必ず参照すること。

## 7. Anomaly-First Rule（異常検知・恒久対策プロトコル）

AIはタスクの早期完了よりも、システムの健全性・正確性・再現可能性を優先する。
Exit 0、HTTP 200、テスト通過などの単一結果だけを完了根拠としてはならない。

### 7.1 Zero-Workaround Policy

異常を隠蔽、回避、別経路で迂回して元タスクを継続することを禁止する。

ただし、以下の目的に限り、非破壊の診断手段を許可する。

- 事象の再現
- 実行対象・環境・権限の確認
- 入力・出力・バイト列・差分の確認
- 真因の特定
- 恒久対策後の再検証

診断手段を使用しても、異常が未解決のまま元タスクへ復帰してはならない。

### 7.2 異常の定義

異常とは、エラーだけでなく、期待状態と観測結果の差分である。

次のいずれかに該当する場合、異常候補として扱う。

1. 内容の正確性
2. 実行結果の正確性
3. 変更範囲の正確性
4. 前提・環境の正確性
5. 完了判定の正確性

### 7.3 異常の分類

- **新規異常**: 即時停止し、Anomaly Gateを適用する。
- **既知・許容済み事項**: Known Caveats、ADR、Issue等の根拠があり、影響・適用範囲・再評価条件が明確な場合のみ継続可。
- **情報・観測結果**: 期待状態との差分がなく、タスク継続を妨げないもの。

AIは、根拠なしに異常を「既知・許容済み」と分類してはならない。

### 7.4 Anomaly Gate

新規異常を検知した場合、以下を記録する。

[Expected]
[Observed]
[Deviation]
[Impact]
[Evidence]
[Root Cause]
[Permanent Fix]
[Reverification]
[Unknown]

Root Causeが仮説段階の間は、確定原因として扱ってはならない。
Root Causeは、仮説・推測・一般論ではなく、基準状態（Git HEAD等）との差分および再現可能な証拠で確定しなければならない。
Permanent Fixの実施とReverificationの成功、または人間による明示的な継続承認があるまで、元タスクへの復帰と完了報告を禁止する。

恒久対策が現時点で実施不能な場合は、
[Unknown] と [Risk] を明示し、人間の継続承認を得るまで保留する。
承認なしに「許容済み」として扱ってはならない。
