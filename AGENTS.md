## AGENTS.md

> このファイルは、このリポジトリでAIエージェントが作業する際の唯一の正典（SSOT）である。作業開始前に必ず読むこと。
> ルールの意図や用語、過去の設計判断に迷った場合は `governance/lexicon.json` と `governance/ADR/` を確認する。推測で埋めない。

---

## 1. プロジェクト概要

> 以下は元のAGENTS.mdの記述から推測した仮の内容です。実際の情報に置き換えてください。

| 項目 | 内容 |
|---|---|
| 概要 | 回収シフト管理・配車支援システム。顧客マスタに基づき、人間の配車判断を支援する |
| フロントエンド | React 19 (TypeScript) |
| データ層 | Supabase (PostgreSQL) |
| ツール層 | Node.js スクリプト（`.agents/scripts/`）、および Antigravity基盤（Skills, Hooks, Subagents） |
| 利用者 | 配車オペレーター |

## 2. よく使うコマンド

| コマンド | 用途 |
|---|---|
| `npm run dev` | 開発サーバー起動（要確認） |
| `npm run build` | ビルド・静的チェック |
| `npm run type-check` | TypeScript型チェック |
| `npm run lint` | Lint（要確認） |
| `npm run test` | テスト実行（要確認） |
| `npm run agent:scan` | 必須ドキュメント（README/DEBT_AND_FUTURE等）とADRの存在チェック |
| `npm run done` | 完了処理。完了コード（GSEAL）を出力 |
| `npx supabase db diff` | スキーマ差分SQLの生成 |

## 3. アーキテクチャ原則

- 依存方向は `components/` → `hooks/` / `utils/` / `services/` → `data/` の一方向。循環参照は禁止。
- 他のstateから導出できる値を `useState` に保存しない。`useMemo` で導出する。
- ライブラリは全体で単一バージョンに統一する（幽霊依存を作らない）。
- 使わなくなった `import` は同じコミットで削除し、`npm run type-check` で確認する。

## 4. 絶対原則（例外なし・全ティア共通）

1. 強制的な一括削除・上書きコマンド（`git clean -fdX` 等）は実行しない。
2. シークレット・APIキー・個人環境固有のパス等をコードや本ファイルにハードコードしない。不明な情報は推測せず「不明」と明示する。
3. 明示的な指示がない限り、既存の関数・UI・データ構造・挙動を削除/リネーム/無効化しない。変更する場合は先に参照元と理由を確認する。
4. 事実確認をせずに実装・提案・結論出しをしない。記憶や一般論での推測は禁止する。コード・ドキュメントの検索だけでなく、必要に応じてブラウザサブエージェント（`/browser`）を用いた実画面での検証など、事実に基づく確認を必ず行うこと。
5. DBスキーマ、認証・権限、決済、および本ファイル自体（統治構造）の変更は、人間の明示的承認なしに実行しない。
6. 設定の読み込みに失敗した場合、デフォルト値へ黙ってフォールバックしない。エラーを出力して停止する。

## 5. リスクベース3段階ルート（ティア）

ROI（投資対効果）を最大化するため、不具合修正や機能変更プロセスを対象の重大度と検証可能性に基づき3段階のルートへ分科します。4章の絶対原則は全ルート共通で適用されます。
なお、ルート判定は自己申告ではなく、パスベースの自動判定（`core_config.json` に定義）とAIリスクスコアリングを優先し、人間のオーバーライドは監査対象となります。

| ルート / ティア | 対象 | 手続きと完了条件 |
|---|---|---|
| **Light (軽微) / T1** | タイポ、文言、UI微調整など。他への影響が極めて限定的なもの。 | 【手続き】修正案の提示と目視確認。<br>【完了条件】人間による目視確認 ＋ `.agents/scratch/worklog.md` への1行ログとDiffリンクの記録（最小トレース）。※月次で無作為抽出（5〜10%）の事後監査対象。即実行し完了後に一言報告。 |
| **Standard (標準) / T2** | 通常の機能不全、ロジック誤り。既存機能へ影響があるもの。 | 【手続き】再現 → 修正 → 既存CI通過のループ。手動承認や過度なドキュメント作成を廃止し自動実行を優先。<br>【完了条件】**バグを再現する新規回帰テストの追加** ＋ 既存CI通過 ＋ AIによる自己検証。※回帰テストの作成は20% ROIガードレールの対象外（投資）とする。 |
| **Strict (厳格) / T3** | 金銭、個人情報、セキュリティ、基幹処理に関わる重大バグ、統治構造・DB・決済の変更。 | 【手続き】完全な修正手順を適用し、独立検証プランの策定を必須とする。`[State]/[Decision]/[Reason]/[Risk]/[Unknown]` で提案。<br>【完了条件】独立検証基準 (Condition C) ＋ 人間による監査承認（`y`） ＋ `governance/ADR/` への記録。 |

ティアの正式な判定基準および強制T3パスの定義は `governance/core_config.json` を正とします。T1/T2は自然な会話で簡潔に報告し、SDR形式を使うのはT3の提案時のみとします。
シミュレーション・パイロット運用中は、誤分類率等のテールリスクKPIを追跡します。

## 6. 変更を行う手順（Standard/Strict 向け）

1. **調査**: 対象範囲・呼び出し元/先・影響するstateを確認する。推測や記憶に頼らず、必ずコード検索等で実在を確認すること。
2. **計画**: T2以上は変更方針を一言でまとめる。
3. **実装**: 計画に沿って変更する。目的と無関係な修正を混在させない。
4. **検証**: Standard (T2) ルートでは既存CIとAIの自己検証ループで完了とみなします。Strict (T3) ルートでは独立した検証基準（Condition C）を満たすこと。
5. **差分監査**: 意図した変更のみが行われ、既存機能が残っているかを確認する。
6. **報告**: 何を・なぜ・どう検証したかを簡潔に伝える。

- 検証に失敗した場合、その場で当て推量の修正を繰り返さず、原因と修正方針を報告してから再修正する。
- T2以上では `.agents/scratch/worklog.md`（1ファイルのみ、上書き可）に対象/方針/検証結果/残課題を記録する。判断の根拠は実際のビルド/テスト結果であり自己申告ではない。

## 7. 自動検査困難な不具合の代替判定と 20% ROI ガードレール

テスト記述や自動化が困難なバグ（フレーク、タイミング依存、UIの違和感など）に対しては、「進捗宗教（偽の証拠を量産すること）」を避けるため、以下の代替基準と制約を設けます。

- **統計的判定とカナリアリリース**: パフォーマンスやタイミングの問題は、本番環境の一部展開やエラー率の比較等をもって判定する。
- **人間によるエビデンス（限定的適用）**: UI/UX修正に限り、スクリーンショットやログを人間が確認する。

> **【20% ROI ガードレール】**
> エビデンス（スクリーンショット、ログ収集、ブラウザエージェントでの過度な検証ループ等）の作成・確認コストが、修正に要した総時間（労力）の **20%** を超えると判断される場合、そのケースは強制的に **「Light (軽微) ルート / T1」** へ再評価（格下げ）するか、判定基準を大幅に簡略化しなければならない。「証拠のための証拠」作成は厳禁とする。

## 8. ドキュメントと負債の管理

- `src/` の変更でユーザーに見える挙動（UI文言・操作方法等）が変わった場合、同じコミットで `README.md` を更新する。更新不要な場合はコミットメッセージにその理由を書く。
- 今回対応しなかった要求・技術的負債は `DEBT_AND_FUTURE.md` に追記する。解消したら同時に削除する。負債の記録自体にペナルティは設けない。正直な記録を優先する。

## 9. プロダクト固有ルール

- 回収案件は、スポット案件を含め、すべて顧客マスタに紐づける。マスタに存在しない孤児データは許可しない。未配車リスト等は必ず顧客マスタと照合し、存在しないものは非表示・破棄する。
- 本システムの目的は完全自動化ではなく、人間の意思決定支援である。発生頻度が極めて低いケース（例: 年数回）のための過剰な自動化ロジックは避け、直感的な手動一括操作UIを優先する。

## 10. 環境固有ルール

- スキーマ変更時は `npx supabase db diff` を実行し、生成SQLを提示のうえ `SCHEMA_HISTORY.md` に記録する。
- Supabase CLI・直接接続の前に `knowledge/supabase_cli_ipv6_pooler_fix/artifacts/manual.md` を確認する。

## 11. 完了条件

- 実装完了時は `npm run done` を実行し、出力された完了コード（GSEAL）を報告に添える。この時、監査履歴は自動的に `.agents/scratch/AMPLOG.jsonl` に追記される。
- push前に `git status` を確認し、意図しないファイルが含まれていないかを確認する（`.agents/scripts/reflect.js --purge` があれば使用）。
- T3の変更は `governance/ADR/` に記録する。本ファイル（ドキュメント）の制約を変更する場合は、それを取り締まる `.agents/scripts/` 側のスクリプトも同一のT3タスク内で同時に修正し、矛盾を生じさせないこと。

## 12. このファイルの保守

- ルールを追加する前に、既存ルールとの重複がないか確認する。似た内容は新設せず統合する。
- 可能な限り「スクリプトで機械的に検査できる形」に落とし込み、散文的な努力目標を増やさない。
- 定期的に（例: 半年に一度）使われていない・守られていないルールを棚卸しする。