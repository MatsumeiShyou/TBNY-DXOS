# Worklog

## 対象
`src/hooks/useDataStore.js`

## 方針
保存済みの日次データ（`dailyState`）をロードする際に、
1. 最新の顧客マスタで新規にスケジュール対象となったジョブ（不足分）を抽出し、`finalPending` に安全に追加（マージ）する。
2. 過去に自動生成されて未配車リストに残っているジョブ（`gen_`）のうち、最新マスタのスケジュール条件から外れたもの（ノイズ）は自動削除する。
既存の手動配置ジョブや手動追加スポット案件（`gen_`以外）は維持し、意図しない破壊を防ぐ。

## 検証結果
- コード修正後、`npm run build` を実行してエラーなしを確認（Green）。
- `expectedJobs` と `existingGenCustomerIds` の依存関係・順序が正しく解決されていることを確認。

## 残課題
なし。
