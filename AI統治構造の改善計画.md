# **複雑系AIエージェントシステムにおける統治構造の再構築と自律型ループエンジニアリングの適用**

現代のソフトウェアエンジニアリングにおいて、AIエージェントの自律性が高度化するにつれ、システムに対するガバナンスの維持は単なる自然言語によるガイドラインの提示から、インフラストラクチャレベルでの「決定論的（Deterministic）な強制機構」の実装へとパラダイムシフトを遂げています。提供された統治アーティファクト（governance\_artifact.md）は、GaC（Governance as Code）やF-SSOT（Functional Single Source of Truth）、リスクティア方式（T1/T2/T3）といった極めて先進的な思想を内包しており、強固な基盤を備えています1。  
しかしながら、AIエージェントの中核を成す大規模言語モデル（LLM）は本質的に確率論的（Probabilistic）な推論エンジンであり、プロンプトや散文形式のルール（絶対原則等）への依存のみでは、コンテキストの膨張や注意力の散漫によるポリシー違反を完全に防ぐことは不可能です2。本報告書は、「AI時代のエンジニア生存戦略」の核心である「情報の網羅的な記憶から、生成された情報の妥当性を判断するレビュー能力への転換」という知見に基づき4、最新のエージェント・アーキテクチャ（PreToolUse / PostToolUseフックの活用、動的サブエージェントによるコンテキスト隔離）を統合した包括的なリサーチ結果を提示します5。既存の絶対原則を一切破壊することなく、現在の統治構造を世界最高水準の堅牢性と開発効率へと昇華させるための緻密な分析と戦略を展開します。

## **1\. 統治構造の脆弱性とリスク分析**

現在の統治構造は、多数のポリシーと自動化ゲート（GATE-1, 2, 3）を定義していますが、エージェントの確率論的な挙動と、それを制御する決定論的なゲートの間に解像度の不一致が存在します。セキュリティ監査者、DevOpsエンジニア、AIオーケストレーターの3つの専門的視点から、既存のgovernance\_artifact.mdに潜むデッドロック、無限ループ、およびセキュリティの抜け漏れに関するリスクを抽出し、以下の表に構造化しました1。

| 視点 | 抽出されたリスク | 発生条件 | 深刻度 |
| :---- | :---- | :---- | :---- |
| **セキュリティ監査者** | **再帰的フック強制（Recursive Hook Enforcement）の欠落による絶対原則の崩壊** | 親エージェントが委譲を行い、サブエージェント（react-builder等）が独自のコンテキストでrun\_commandやwrite\_to\_fileを実行した際。 | クリティカル |
| **セキュリティ監査者** | **ダミー実装に起因する脆弱コードの無検証注入（GATE-2の形骸化）** | T2/T3タスクにおいて、エージェントがファイルシステムへの書き込み（write\_to\_file）を実行した直後。 | 高 |
| **DevOpsエンジニア** | **自己修復ループの欠如による検証失敗時の無限ループ（当て推量修正の誘発）** | ツール実行後に予期せぬエラーや静的解析違反が発生し、エージェントがプロンプトの指示のみに頼って自律的に解決を図る際。 | 高 |
| **DevOpsエンジニア** | **異種データソースの未正規化による不整合とドメインルール（PROD-1）違反** | エージェントが状態の読み取りツールを実行し、その結果（Unixタイムスタンプ等）をモデルが直接解釈するプロセス。 | 中 |
| **AIオーケストレーター** | **コンテキスト・スプロール（肥大化）による記憶喪失と絶対原則の忘却** | 長時間のセッションや、複数のファイルにまたがるT2/T3変更タスクを単一のエージェント内で継続実行した際。 | 高 |
| **AIオーケストレーター** | **情報不足時の例外フローにおける硬直化と早期デッドロック** | AGENTS.mdやプロダクト固有ルール解釈に必要な前提知識が不足し、かつ人間の承認が得られない状況下。 | 中 |

これらの抽出されたリスクは、根本的に「ルールの存在」と「ルールの強制」のギャップから生じています。例えば、セキュリティ監査者の視点で最も致命的なのは、親エージェントがサブエージェントを生成した際、既存の自動化ゲート（.agents/hooks.json）が子プロセスに対しても再帰的に適用されるかどうかがアーティファクト上で定義されていない点です。現在のアーティファクトの記述では基盤となるフックエンジンが再帰的伝播をサポートしているか不明確なため確認が必要ですが、もし適用されない場合、サブエージェントはABS-1（破壊的操作の禁止）を完全にバイパスして本番環境の破壊やシークレットの流出を引き起こす危険性を孕んでいます1。  
また、DevOpsエンジニアの視点からは、エージェントの検証ループにおけるフィードバックの欠如が深刻なボトルネックとして浮上します。ADR-001において「当て推量の修正ループ」が厳格に禁止されているにもかかわらず、GATE-2（post-write-check.js）がESLint未導入のダミー実装であるため、エラー原因がエージェントに構造的コンテキストとしてフィードバックされません1。結果として、エージェントは盲目的な試行錯誤を強いられ、無限ループやビルドのデッドロックに陥る構造的な弱点を抱えています。

## **2\. 推奨される改善アーキテクチャと知見**

前述の脆弱性を克服するため、「AI時代のエンジニア生存戦略」が提唱する「プロンプトによる確率的なお願い」から「フックによる決定論的なインターセプト」への移行パラダイムを適用します4。既存のAGENTS.mdの絶対原則やリスクティア（T1/T2/T3）を無効化するような破壊的変更は一切行わず、それらのルールをインフラストラクチャの層で機械的に強制するためのアプローチを各視点ごとに2つずつ立案しました。

| 視点 | 提案する改善アプローチ（適用技術） | 導入のメリット | 導入のデメリット | 確信度 |
| :---- | :---- | :---- | :---- | :---- |
| **セキュリティ監査者** | **1\. 動的サブエージェントに対する再帰的フック（Recursive Hook Enforcement）の強制** | サブエージェントの暴走や絶対原則（ABS-1, 2）の意図的・偶発的なバイパスを数学的に不可能にする。 | サブエージェントの全アクションでフックが同期実行されるため、処理が重い場合はシステム全体のレイテンシが悪化する。 | 極めて高い |
| **セキュリティ監査者** | **2\. 決定論的セキュリティ・シールドとしての軽量PreToolUse正規表現スキャン** | クレデンシャルや破壊的コマンドが実行・保存される「前」にミリ秒単位でインターセプトし、確実な防御壁を構築する。 | 厳密すぎる正規表現ルールは、テスト用のモックデータや安全なシェルコマンドを誤検知（False Positive）でブロックするリスクがある。 | 高い |
| **DevOpsエンジニア** | **3\. PostToolUseフィードバック注入による決定論的「自己修復ループ」の構築** | エラーの行番号や詳細（eslint \--fixの結果等）を即座にコンテキストへ注入し、当て推量を排除した最短の修正を実現する。 | フックの同期実行に時間がかかる（5秒以上など）と、エージェントの推論ループ全体が深刻な遅延を起こす。 | 極めて高い |
| **DevOpsエンジニア** | **4\. PostToolUseフックによるデータ・ノーマライゼーション（正規化）の適用** | 生データ（Unix時間等）をフック層で統一フォーマットに変換し、AIのパースミスやPROD-1違反（孤児データ発生）を根絶する。 | スキーマや外部APIの仕様変更のたびに、フック側の正規化スクリプトも継続的にメンテナンスする必要が生じる。 | 高い |
| **AIオーケストレーター** | **5\. 動的タスク委譲と「使い捨て」サブエージェントによるコンテキスト隔離** | コンテキスト・スプロールを防ぎ、エージェントの注意力を特定ファイルと絶対原則のみに集中させることでハルシネーションを防止する。 | サブエージェントへの指示（ブリーフィング）の質が親エージェントのプロンプト生成能力に強く依存するようになる。 | 高い |
| **AIオーケストレーター** | **6\. PreCompactフックを用いた忘却ログの可視化と外部記憶への自律的退避** | コンテキスト圧縮時に失われる重要情報を検知し、ファイルシステム（worklog.md）へ退避させることで完全な記憶の連続性を担保する。 | プラットフォーム（CLIツール等）の仕様に依存するため、環境によっては同等のライフサイクルイベントの代替実装が必要になる。 | 中〜高 |

### **多角的な分析と深い技術的洞察**

**セキュリティ監査者の視点：境界防御と再帰的統制** AIエージェントが自律的に複数のツールを駆使する環境において、セキュリティの担保は「エージェントにルールを記憶させること」ではなく「エージェントがルールを破れないインフラを構築すること」に依存します3。現在定義されている絶対原則（ABS-1：強制的一括削除禁止など）は、safety-check.jsという実行前ゲート（GATE-1）によって部分的に守られていますが、これがbashのような特定のシェルツールにのみ適用されているのか、apply\_patchやMCP（Model Context Protocol）を介したファイル操作ツール全体に適用されているのかが、現在のgovernance\_artifact.mdの記述からは不明確なため確認が必要です1。もしシェルツールのみに限定されている場合、エージェントがファイル直接編集ツールを用いた瞬間にセキュリティゲートは迂回されてしまいます8。  
これを解決する最初のアプローチが「決定論的セキュリティ・シールドとしての軽量PreToolUse正規表現スキャン」です。すべての書き込み操作やコマンド実行の前に発火するPreToolUseフックに、極めて軽量な正規表現チェッカーを配置します。ここで重要なのは、エージェントの意図をLLMで再評価するのではなく、純粋なプログラムとしてパターンマッチを行い、違反があれば即座にdeny（拒否）ステータスをエージェントに突き返すことです2。エージェントはこの拒否理由（例：「ABS-2違反：APIキーに類似する文字列のハードコードを検知」）を受け取り、自律的に安全な代替案を模索します。さらに、もう一つのアプローチとして「再帰的フック強制」を導入します。T2やT3のタスクで親エージェントがreact-builderなどのサブエージェントを起動した際、親に設定された防御壁が子プロセスにも透過的かつ強制的に適用されなければなりません3。これにより、いかに複雑なエージェントの群れ（スウォーム）が形成されても、単一の決定論的な統治構造のもとに制御することが可能となります。  
**DevOpsエンジニアの視点：自己修復とデータの純化** 開発効率と品質保証の観点から見ると、AIエージェントの最大のボトルネックは「失敗時の不確実なリトライ」にあります。現在の統治構造ではADR-001によって「当て推量修正ループ」が禁止されていますが1、エラーの具体的な原因がエージェントに伝達されなければ、この禁止ルール自体が作業の進行を完全に停止させるデッドロック要因となります。これを打破するアプローチが「PostToolUseフィードバック注入による決定論的『自己修復ループ』」です。  
未実装の技術的負債となっているpost-write-check.js（GATE-2）を真のPostToolUseフックへと昇華させます1。エージェントがファイルの書き込みを完了した直後、システムは非同期ではなく同期的に該当ファイルに対してeslintやtsc（将来的なTypeScript導入を見据えて）を実行します。もし静的解析エラーが発見された場合、フックは単に処理を中断するのではなく、そのエラー出力の標準出力（stdout）を捉え、JSON形式のadditionalContextとしてエージェントの現在のコンテキストへ直接注入します2。これにより、エージェントは「行番号42で循環参照エラーが発生している」という決定論的な事実（ファクト）を受け取り、プロンプトの指示に依存しない自律的かつ確実なコード修復を同一の推論ループ内で完了させることができます2。  
さらに、DevOpsの観点からは「PostToolUseフックによるデータ・ノーマライゼーション（正規化）」も不可欠です。本プロジェクトは将来的にSupabaseへの移行を予定しており、現在はLocalStorageとAPIによるデュアル書き込みが行われています1。異なるデータソースから読み取られた状態（State）をエージェントが処理する際、日付フォーマットやステータスコードの揺れをモデル自身の推論能力でパースさせると、必ず確率的な解釈ミス（ハルシネーション）が発生します7。これを防ぐため、データの読み取りツール（Read）が結果を返す直前にPostToolUseフックを介入させ、すべてのデータをISO 8601形式などに強制変換してからエージェントに渡します。これにより、PROD-1（顧客マスタ紐づけ必須）などのドメインルールを処理する際のエージェントの精度が飛躍的に向上します1。  
**AIオーケストレーターの視点：コンテキストの経済学と忘却の防止** T3に分類されるような高リスクかつ広範な変更タスクにおいて、AIエージェントは膨大なコードベースの検索、方針立案、ファイル編集、そして検証という長いサイクルを単一のセッションで実行します。この時、LLMのコンテキストウィンドウが肥大化（コンテキスト・スプロール）し、セッション初期に読み込んだはずのAGENTS.mdの絶対原則や、ユーザーと合意したはずの作業方針を忘却するという致命的なリスクが生じます1。  
この問題に対処するアプローチが「動的タスク委譲と『使い捨て』サブエージェントによるコンテキスト隔離」です。親エージェントは調査と計画（COG-L1）のみに特化し、実際の実装フェーズに入った段階で、タスクツールを用いて個別のファイル編集に特化したサブエージェント（例：react-builder）を動的に生成します5。このサブエージェントは過去の長大な対話履歴を一切持たず、親から渡された「現在のタスクに必要な最小限のブリーフィング」と「絶対原則」のみをクリーンなコンテキストとして保持して起動します3。このアーキテクチャは、コンテキストの肥大化を防ぎ、モデルの注意力を極限まで高め、既存機能の無断変更（ABS-3違反）を根本から断ち切る最良の手法です3。  
同時に、長時間のオーケストレーションを支えるもう一つの柱が「PreCompactフックを用いた忘却ログの可視化と外部記憶への自律的退避」です。エージェントのメモリが限界に近づき、古いコンテキストが圧縮・破棄されようとする直前に発火するライフサイクルフック（PreCompact）を監視します2。ここで失われようとしている情報の中に、進行中のSDR（State/Decision/Reason）フローの前提条件などが含まれていた場合、システムはそれを自動的に.agents/scratch/worklog.mdというファイルシステム上の永続層へ書き出します。エージェントにとっての真の記憶はLLMのコンテキスト内ではなく、ファイルシステム上に存在すべきであるというアーキテクチャの原則を体現するものです3。

### **矛盾の特定とアーキテクチャの合成：多層ゲートウェイパターンの確立**

立案された改善案をシステム全体に統合しようとする際、決定的なトレードオフ（矛盾）が顕在化します。それは「セキュリティ・品質を担保するための決定論的フックの重量」と「自律型ループエンジニアリングにおける推論スピード（開発効率）」の衝突です。  
PostToolUseフックにおいて、エージェントがコードを1行書き換えるたびにプロジェクト全体のビルド（npm run build）やE2Eテストを実行すれば、システムは数学的に完璧な品質を維持できます。しかし、フックの実行は同期的であるため、エージェントは毎回数十秒から数分間フリーズすることになり、軽快な試行錯誤や「完了しない学習」のサイクルが完全に崩壊します2。一方で、パフォーマンスを優先してフックを軽量化しすぎると、クリティカルな不具合が完了前ゲート（GATE-3）まで素通りしてしまい、T3の人間承認プロセスの負荷を増大させます。  
この矛盾を両立させ、既存の統治構造（governance\_artifact.md）の理念を損なうことなくシステムを昇華させるための最適なアーキテクチャパターンとして、検証処理をライフサイクル（時間軸）に応じて三層に分割・オフロードする「Harness（ハーネス）に基づく多層ゲートウェイパターン」を合成・結論づけます3。

> 1. **第一層：ミリ秒単位の事前拒否層（PreToolUse Interception）** ツールが実行される直前の層です。ここでは推論や重い静的解析は一切行いません。safety-check.jsを拡張し、純粋な正規表現マッチングやコマンドのブラックリスト検証のみを行い、ABS-1（破壊的操作）やABS-2（シークレットのハードコード）に抵触する行動をミリ秒単位で検知し、即座にdenyとしてブロックします。エージェントの思考速度を一切妨げずに致命傷を防ぐ最前線の盾となります2。  
> 2. **第二層：秒単位の局所フィードバック層（PostToolUse Quality Gate）** ツールが実行された直後の層です。ここではプロジェクト全体の検証は行わず、エージェントが「たった今書き込んだ単一ファイル」のみをスコープとして、軽量なLinter（eslint \--fix）やフォーマッターを同期実行します。実行時間を厳格に5秒以内に制限し、発見されたエラーをadditionalContextとして注入します。これにより、開発効率を損なうことなく、局所的なロジックの完全性を自己修復ループ内で担保します2。  
> 3. **第三層：タスク単位の全体検証層（Stop Hook / Closure Gate）** エージェントが一連のループを終え、タスクの完了を宣言する（npm run doneやStopイベント）タイミングの層です。ここで初めて、既存のclosure\_gate.jsやdone.jsと連動し、数分を要するプロジェクト全体のフルビルド、依存関係の完全性チェック、worklog.mdの書式検証、そしてADRの連番整合性チェック（scan.jsの呼び出し）を実行します1。この層はエージェントの微細なループには介入せず、最終的な完了証明（GSEALコード）の発行可否のみを決定する重厚なゲートとして機能します。

この三層構造のハーネスパターンを適用することで、エージェントは制約の中で最大のスピードを発揮しつつ、システムの完全性はインフラレベルで保証されるという、真の自律型エンジニアリング基盤が完成します2。

## **3\. 実装に向けた段階的アクションプラン**

既存の統治アーティファクト（governance\_artifact.md）に記述された人間中心主義と、本番稼働中の環境を破壊しないために、システムへの変更は段階的なセーフガードを伴うフェーズ分けの手法（Progressive Enhancement）で実装します。絶対原則（ABS-1〜6）を無効化するようなスクリプトの直接編集は行わず、統治スクリプト群（.agents/scripts/）の拡張を通じて決定論的な振る舞いを注入します1。

### **フェーズ1：可観測性の確立と影の監視（リスクゼロの導入）**

最初のステップでは、エージェントの実際のワークフローを一切ブロックせず、フックの挙動とイベントの発生状況を観測するだけの「シャドーモード」を導入します。

* **Step 1.1:** .agents/hooks.jsonを編集し、現在のすべてのPreToolUseおよびPostToolUseイベントをフックする軽量なロガースクリプト（例：.agents/scripts/hook-observer.js）を登録します。  
* **Step 1.2:** このスクリプトは、受け取ったイベントコンテキスト（ツール名、引数、実行者）をAMPLOG.jsonlの別ストリーム、または一時的な監視ログにJSON形式で追記します。  
* **セーフガード:** この監視スクリプトは、内部でどのような例外が発生しても必ず終了コード0（allow）を返すように構成します。これにより、万が一スクリプトにバグがあってもエージェントの作業は一切阻害されません2。  
* **Step 1.3:** T1（低リスク）タスクを発行し、親エージェントに意図的にサブエージェント（react-builder）を起動させます。ログを分析し、サブエージェントのツール呼び出しに対してもPreToolUseイベントが発火しているか（再帰的フック強制が環境でサポートされているか）を確定させます。

### **フェーズ2：局所的フィードバックループの有効化（開発効率の向上）**

技術的負債として記録されているダミー実装のpost-write-check.jsを、真の自己修復ゲートへと置き換えます1。

* **Step 2.1:** .agents/scripts/post-write-check.jsを改修し、エージェントから渡されたファイルパスの引数を受け取れるようにします。  
* **Step 2.2:** スクリプト内で、対象となる単一ファイルに対してのみnpx eslint \--fix \<filepath\>を実行するロジックを追加します。実行時間が5秒を超えた場合は強制終了するタイムアウト処理を必ず実装します2。  
* **Step 2.3:** ESLintがエラーを出力した場合、その標準出力をキャプチャし、プラットフォームの仕様に合わせたJSON形式（例：additionalContextフィールドを含むオブジェクト）で標準出力へ返し、エージェントにコンテキストとして注入します2。  
* **セーフガード:** エラーが検出されても、スクリプトの終了コードは1（警告）とし、ファイルの書き込み自体はロールバック（元に戻す）しません。これにより、エージェントは「変更後の不完全なファイル」と「それに対する具体的なエラーメッセージ」の両方を手元に残し、次のループで的確に修正することが可能になります3。

### **フェーズ3：コンテキスト隔離を前提とした動的オーケストレーションの標準化**

エージェントの記憶喪失を防ぐため、タスク委譲のルールをスキル定義に明文化し、自律的なコンテキスト隔離を促します。

* **Step 3.1:** 既存のスキル定義ファイル（.agents/skills/governance-workflow/SKILL.mdおよびreact-vite-dev/SKILL.md）を更新します1。  
* **Step 3.2:** T2およびT3タスクにおいて「複数ファイルにまたがる実装や、未知のコンポーネントへの介入が必要な場合は、親プロセスで直接編集ツールを使用せず、必ず対象ファイルごとに限定されたサブエージェントをTaskツール経由で起動し、委譲すること」というプロンプトベースの規約を追記します5。  
* **Step 3.3:** サブエージェントへのブリーフィングテンプレートを定義し、「AGENTS.mdの絶対原則を遵守せよ」という一文を必ず子プロセスの初期コンテキストに含めるよう指示します3。  
* **セーフガード:** この変更はあくまでAI向けのプロンプト（スキル定義）のレベルにとどまるため、governance\_artifact.mdの統治ルールそのものや、人間の承認プロセス（SDRフロー）には一切の副作用をもたらしません。

### **フェーズ4：決定論的防御壁の完全稼働とクロージャ統合（セキュリティの完成）**

システムの安定稼働が確認できた後、最終的なインターセプト（拒否）機能を有効化し、絶対的な防御壁を完成させます。

* **Step 4.1:** .agents/scripts/safety-check.js（GATE-1）を拡張し、従来のコマンドチェックに加えて、write\_to\_fileおよびapply\_patch実行時のペイロード（書き込み内容の文字列）に対する正規表現スキャンを実装します。APIキーのパターンや、破壊的なSQLクエリの断片を検知対象とします。  
* **Step 4.2:** 違反を検知した場合、フックは明確な違反理由（例：「ABS-2違反：シークレットの疑いがある文字列を検知」）を出力し、終了コード2（またはプラットフォームが指定するブロックコード）を返して処理を完全にインターセプト（deny）します3。  
* **セーフガード:** 正規表現による誤検知（False Positive）で開発が停止する事態を防ぐため、既存のバイパス機能である// gov-bypassタグのロジックは維持します1。ただし、バイパスが使用された場合はフック側でこれを検知し、AMPLOG.jsonlに対して警告フラグ付きの特別ログを自動追記するよう監視を強化します。  
* **Step 4.3:** 最後に、これらの細かいフックの実行履歴が、タスク完了時に実行されるclosure\_gate.js（GATE-3）によって最終検証されるよう結合します。エラーの無視やバイパスの乱用があった場合、GSEAL（完了証明）の発行を阻止し、人間によるT3承認フローへのエスカレーションを機械的に強制します1。

本アクションプランの遂行により、既存の統治アーティファクトは単なる静的なドキュメントから、エージェントの推論と物理的な実行環境の境界で絶えず脈動し、違反を自動的に弾き返す「生きたインフラストラクチャ」へと進化を遂げます。

#### **引用文献**

> 1. governance\_artifact.md  
> 2. Claude Code Hooks: The Deterministic Control Layer for AI Agents \- Dotzlaw Consulting, [https://dotzlaw.com/insights/claude-hooks/](https://dotzlaw.com/insights/claude-hooks/)  
> 3. Agent Architecture: Building AI-Powered Development Harnesses \- Blake Crosley, [https://blakecrosley.com/guides/agent-architecture](https://blakecrosley.com/guides/agent-architecture)  
> 4. AI時代のエンジニア生存戦略｜NotebookLMで「完走しない学習」を資産に変える3ステップ活用術, [https://techplay.jp/event/993441](https://techplay.jp/event/993441)  
> 5. Claude Code Orchestration: A Summary of Units, Sub-agents, and Hooks (For Engineers), [https://note.com/hayanomc2015/n/nc6e427654312?hl=en](https://note.com/hayanomc2015/n/nc6e427654312?hl=en)  
> 6. Introducing AI Coding Agent Governance: Using Hooks to Bring Visibility to the ADLC | Blog, [https://www.endorlabs.com/learn/introducing-agent-governance-using-hooks-to-bring-visibility-to-ai-coding-agents](https://www.endorlabs.com/learn/introducing-agent-governance-using-hooks-to-bring-visibility-to-ai-coding-agents)  
> 7. 1.5 — Agent SDK Hooks | Claude Certification Guide, [https://claudecertificationguide.com/learn/1-agentic-architecture/1-5-agent-sdk-hooks](https://claudecertificationguide.com/learn/1-agentic-architecture/1-5-agent-sdk-hooks)  
> 8. Codex CLI Hooks Reference — hooks.json, PreToolUse & PostToolUse, [https://agenticcontrolplane.com/blog/codex-cli-hooks-reference](https://agenticcontrolplane.com/blog/codex-cli-hooks-reference)  
> 9. Hooks reference \- Claude Code Docs, [https://code.claude.com/docs/en/hooks](https://code.claude.com/docs/en/hooks)  
> 10. Agent Sprawl and Context Sprawl: Causes, Risks, Fix \[2026\] \- Atlan, [https://atlan.com/know/ai-agent/agent-sprawl/](https://atlan.com/know/ai-agent/agent-sprawl/)