# テンプレート編集機能（プレビューモード）の制限解除

プレビューモード（紫色）中にサイドバーメニューを開き、本番同様にマスタデータの編集や追加を行えるように制限を解除します。

## User Review Required

プレビュー中にマスタ（顧客・車両・作業員など）を編集・追加した場合、その変更は**本番のマスタデータとして直ちに保存されます**。これは本番環境と同じ挙動であり問題ありませんが、念のためご認識ください。

また、「スポット案件」に関しては、あくまで「その日限りの例外処理」であるため、これまで通りテンプレートへの保存はできない（未配車リストにロックされて表示される）仕様を維持します。

## Open Questions

特になし。

## Proposed Changes

### src/components/Header.tsx

#### [MODIFY] Header.tsx
- メニュー（≡）ボタンの disabled 属性および className のスタイル条件から isPreviewMode を除外し、プレビュー中でもサイドバーを開けるように変更します。

## Verification Plan

### Manual Verification
1. テンプレートの「プレビュー読込」を行う。
2. 左上のメニュー（≡）がクリックできることを確認する。
3. 顧客マスタ管理を開き、新しい顧客を追加（または既存顧客を編集）して保存する。
4. プレビュー中の未配車リストにその顧客が即座に反映されることを確認する。
5. 「テンプレート上書き保存」を行い、追加した顧客がテンプレートに保存されることを確認する。
